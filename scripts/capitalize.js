// Arrays 
// Functions 
// "objects"
const who_am_i = `i'm batman`;

const first = who_am_i.slice(0,1).toUpperCase();

const rest = who_am_i.slice(1);

console.log(first + rest);

console.log(`The length of the string is ${who_am_i.length}`);
